# Project Instructions

This file is a router. Pick the role that matches you:

- **Using re-frame-pair to debug a host re-frame app?** Stop here. Read [`SKILL.md`](SKILL.md) — it's the skill manifest the Claude Code skill system loads, and it points at [`docs/skill/`](docs/skill/) for detail-on-demand. Maintainer-only instructions (Beads workflow, mandatory `git push` session-completion, fixture integration loop) would misfire in a host project.

- **Maintaining the re-frame-pair skill itself?** Read [`CONTRIBUTING.md`](CONTRIBUTING.md) — Beads workflow, build/test commands, fixture integration loop, session-completion protocol, shell-non-interactivity tips.

This file holds no authoritative instructions; the two routes above are the only sources of truth.
